<Window x:Class="POS.Client.Views.CashierWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cashier" Height="700" Width="1100"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1.2*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Products -->
        <Grid Grid.Column="0" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Products" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

            <ComboBox x:Name="cbVariants" Grid.Row="1" Margin="0,0,0,10" 
                      SelectionChanged="cbVariants_SelectionChanged"
                      DisplayMemberPath="Name"/>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="icProducts">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Width="150" Height="100" Margin="5" Click="ProductButton_Click" 
                                    Tag="{Binding ServerId}" Background="#FF3C3C3C" Foreground="White">
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding BasePrice, StringFormat='{}{0:F2} THB'}" Foreground="#FF90EE90" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <!-- Right: Cart -->
        <Grid Grid.Column="1" Background="#FFF5F5F5" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Cart" FontSize="20" FontWeight="Bold" Margin="10"/>

            <ListBox x:Name="lbCart" Grid.Row="1" Margin="10"/>

            <TextBlock x:Name="txtSubtotal" Grid.Row="2" Text="Subtotal: 0.00" Margin="10" FontSize="16"/>
            <TextBlock x:Name="txtTax" Grid.Row="3" Text="Tax: 0.00" Margin="10" FontSize="14" Foreground="Gray"/>
            <TextBlock x:Name="txtTotal" Grid.Row="4" Text="Total: 0.00" FontSize="24" 
                       FontWeight="Bold" Margin="10" HorizontalAlignment="Right"/>

            <Button x:Name="btnPay" Grid.Row="5" Content="PAY CASH" Height="60" Margin="10" 
                    Background="#FF28A745" Foreground="White" FontSize="20" Click="btnPay_Click"/>
        </Grid>
    </Grid>
</Window>