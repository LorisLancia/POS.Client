<Window x:Class="POS.Client.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="350" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Background="#FFF0F0F0">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock Text="POS Login" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Username" Margin="0,5"/>
            <TextBox x:Name="txtUsername" Height="30" Padding="5"/>

            <TextBlock Text="PIN" Margin="0,5"/>
            <PasswordBox x:Name="txtPin" Height="30" Padding="5"/>

            <TextBlock Text="Store ID" Margin="0,5"/>
            <TextBox x:Name="txtcompanyId" Height="30" Padding="5" Text="1"/>

            <Button x:Name="btnLogin" Content="Login" Height="35" Margin="0,20,0,0" 
                    Background="#FF007ACC" Foreground="White" Click="btnLogin_Click"/>

            <TextBlock x:Name="txtMessage" Foreground="Red" Margin="0,10" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Window>